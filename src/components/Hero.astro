---
// Componente Hero para la página principal de ozonoterapia
---

<section id="hero" class="bg-gradient-to-br from-blue-50 to-green-50 py-16 px-4 transition-all duration-500 relative overflow-hidden">
  <!-- Bubble Background -->
  <div class="bubble-background">
    <div class="bubble bubble-1"></div>
    <div class="bubble bubble-2"></div>
    <div class="bubble bubble-3"></div>
    <div class="bubble bubble-4"></div>
    <div class="bubble bubble-5"></div>
    <div class="bubble bubble-6"></div>
    <div class="bubble bubble-7"></div>
    <div class="bubble bubble-8"></div>
    <div class="bubble bubble-9"></div>
    <div class="bubble bubble-10"></div>
    <div class="bubble bubble-11"></div>
    <div class="bubble bubble-12"></div>
  </div>
  
  <div class="max-w-6xl mx-auto relative z-10">
    <div id="mainContent" class="text-center mb-12">
      <h1 class="text-4xl md:text-6xl font-bold text-gray-800 mb-6 scroll-reveal fade-up">
        <img id="logoMain" style="margin: auto; transition: filter 0.3s ease;" src="images/logo.png" alt="Logo de Save The Date">
      </h1>
      <p id="hero-subtitle" class="text-xl md:text-2xl text-gray-600 mb-8 max-w-4xl mx-auto scroll-reveal fade-up">
        Creamos invitaciones digitales únicas para bodas, quinceañeras, fiestas y eventos especiales
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center scroll-reveal fade-up">
        <button id="portfolio-btn" class="bg-purple-600 hover:bg-purple-700 text-white px-8 py-3 rounded-lg font-semibold transition-all duration-300 hover:scale-105">
          Ver Portafolio
        </button>
        <button id="quote-btn" class="border-2 border-purple-600 text-purple-600 hover:bg-purple-600 hover:text-white px-8 py-3 rounded-lg font-semibold transition-all duration-300 hover:scale-105">
          Solicitar Cotización
        </button>
      </div>
    </div>
    
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div class="space-y-6 scroll-reveal fade-left">
        <h2 id="why-digital-title" class="text-3xl font-bold text-gray-800 mb-6">
          ¿Por qué elegir invitaciones digitales?
        </h2>
        <p id="why-digital-text1" class="text-gray-600 text-lg leading-relaxed">
          Las invitaciones digitales son la forma moderna, elegante y ecológica de invitar a tus seres queridos a eventos especiales. Personalizamos cada diseño según tu estilo y temática.
        </p>
        <p id="why-digital-text2" class="text-gray-600 text-lg leading-relaxed">
          Ofrecemos diseños únicos, animaciones personalizadas y la posibilidad de incluir música, videos y confirmación de asistencia en tiempo real.
        </p>
        <div class="bg-white p-6 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300 scroll-reveal scale">
          <h3 id="specialized-title" class="text-xl font-semibold text-gray-800 mb-3">Especializados en eventos:</h3>
          <ul class="event-list space-y-2 text-gray-600">
            <li class="flex items-center">
              <span class="w-2 h-2 bg-purple-500 rounded-full mr-3"></span>
              <span id="event-weddings">Bodas y Matrimonios</span>
            </li>
            <li class="flex items-center">
              <span class="w-2 h-2 bg-purple-500 rounded-full mr-3"></span>
              <span id="event-sweet15">Quinceañeras</span>
            </li>
            <li class="flex items-center">
              <span class="w-2 h-2 bg-purple-500 rounded-full mr-3"></span>
              <span id="event-birthdays">Fiestas de Cumpleaños</span>
            </li>
            <li class="flex items-center">
              <span class="w-2 h-2 bg-purple-500 rounded-full mr-3"></span>
              <span id="event-corporate">Eventos Corporativos</span>
            </li>
          </ul>
        </div>
      </div>
      
      <div class="relative scroll-reveal fade-right">
        <div class="bg-white p-8 rounded-2xl shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-105">
          <div class="text-center">
            <div class="w-24 h-24 bg-gradient-to-br from-purple-500 to-purple-700 rounded-full mx-auto mb-6 flex items-center justify-center">
              <svg class="w-12 h-12 text-white" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.293l-3-3a1 1 0 00-1.414 0l-3 3a1 1 0 001.414 1.414L9 9.414V13a1 1 0 102 0V9.414l1.293 1.293a1 1 0 001.414-1.414z" clip-rule="evenodd"></path>
              </svg>
            </div>
            <h3 id="personalized-title" class="text-2xl font-bold text-gray-800 mb-4">Invitaciones Personalizadas</h3>
            <p id="personalized-text" class="text-gray-600 mb-6">
              Cada diseño es único y personalizado según tus gustos, colores favoritos y temática del evento. Incluimos animaciones y elementos interactivos.
            </p>
            <div class="grid grid-cols-2 gap-4 text-sm">
              <div class="bg-purple-50 p-3 rounded-lg hover:bg-purple-100 transition-colors duration-300">
                <div id="feature1-title" class="font-semibold text-purple-700">Diseños</div>
                <div id="feature1-subtitle" class="text-purple-600">únicos</div>
              </div>
              <div class="bg-purple-50 p-3 rounded-lg hover:bg-purple-100 transition-colors duration-300">
                <div id="feature2-title" class="font-semibold text-purple-700">Entrega</div>
                <div id="feature2-subtitle" class="text-purple-600">rápida</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Logo styling for dark mode */
  .logo-light {
    transition: filter 0.3s ease;
  }
  
  .logo-dark {
    display: none;
  }
  
  .dark .logo-light {
    filter: invert(1) brightness(2);
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translate3d(0, -100%, 0);
    }
    to {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translate3d(-100%, 0, 0);
    }
    to {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translate3d(100%, 0, 0);
    }
    to {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translate3d(0, 100%, 0);
    }
    to {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }
  }

  @keyframes bounceGentle {
    0%, 20%, 53%, 80%, 100% {
      animation-timing-function: cubic-bezier(0.215, 0.61, 0.355, 1);
      transform: translate3d(0, 0, 0);
    }
    40%, 43% {
      animation-timing-function: cubic-bezier(0.755, 0.05, 0.855, 0.06);
      transform: translate3d(0, -15px, 0);
    }
    70% {
      animation-timing-function: cubic-bezier(0.755, 0.05, 0.855, 0.06);
      transform: translate3d(0, -7px, 0);
    }
    90% {
      transform: translate3d(0, -2px, 0);
    }
  }

  .animate-fade-in-down {
    animation: fadeInDown 1s ease-out;
  }

  .animate-slide-in-left {
    animation: slideInLeft 0.8s ease-out;
  }

  .animate-slide-in-right {
    animation: slideInRight 0.8s ease-out;
  }

  .animate-fade-in-up {
    animation: fadeInUp 1s ease-out;
  }

  .animate-bounce-slow {
    animation: bounceGentle 3s infinite;
  }

  .animation-delay-100 {
    animation-delay: 0.1s;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
  }

  .animation-delay-300 {
    animation-delay: 0.3s;
  }

  .animation-delay-400 {
    animation-delay: 0.4s;
  }

  .animation-delay-500 {
    animation-delay: 0.5s;
  }

  .animation-delay-600 {
    animation-delay: 0.6s;
  }

  .scroll-animate {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.6s ease-out;
  }

  .scroll-animate.animate {
    opacity: 1;
    transform: translateY(0);
  }
  
  /* Estilos específicos para modo oscuro */
  .dark-mode-animated {
    transition: background-color 0.5s ease, color 0.5s ease, transform 0.6s ease, opacity 0.6s ease, box-shadow 0.5s ease !important;
  }
</style>

<script>
  // Intersection Observer para animaciones de scroll
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animate');
      }
    });
  }, observerOptions);

  // Observar todos los elementos con la clase scroll-animate
  document.addEventListener('DOMContentLoaded', () => {
    const scrollElements = document.querySelectorAll('.scroll-animate');
    scrollElements.forEach(el => observer.observe(el));
    
    // Verificar si estamos en modo oscuro y aplicar clases adicionales
    const isDarkMode = document.documentElement.classList.contains('dark');
    if (isDarkMode) {
      document.querySelectorAll('.bg-white, .shadow-lg, .shadow-xl').forEach(el => {
        el.classList.add('dark-mode-animated');
      });
    }
    
    // Inicializar el logo según el modo actual
    updateLogo();
  });
  
  // Función para actualizar el logo según el modo
  function updateLogo() {
    const isDarkMode = document.documentElement.classList.contains('dark');
    const logo = document.getElementById('logoMain');
    if (logo) {
      if (isDarkMode) {
        logo.style.filter = 'invert(1) brightness(2)';
      } else {
        logo.style.filter = 'none';
      }
    }
  }
  
  // Escuchar cambios en el modo oscuro
  const darkModeObserver = new MutationObserver((mutations) => {
    mutations.forEach((mutation) => {
      if (mutation.attributeName === 'class') {
        const isDarkMode = document.documentElement.classList.contains('dark');
        
        // Actualizar elementos de fondo
        document.querySelectorAll('.bg-white, .shadow-lg, .shadow-xl').forEach(el => {
          if (isDarkMode) {
            el.classList.add('dark-mode-animated');
          } else {
            el.classList.remove('dark-mode-animated');
          }
        });
        
        // Actualizar logo
        updateLogo();
      }
    });
  });
  
  darkModeObserver.observe(document.documentElement, { attributes: true });
</script>

<style>

  #mainContent{
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    box-shadow: 0 0px 30px rgba(0, 0, 0, 0.103);
    padding: 2rem;
    border-radius: 1rem;
  }
  .bubble-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
  }
  
  .bubble {
    position: absolute;
    border-radius: 50%;
    opacity: 0;
    pointer-events: none;
    box-shadow: 
      inset -10px -10px 20px rgba(255,255,255,0.3),
      inset 10px 10px 20px rgba(0,0,0,0.1),
      0 0 20px rgba(255,255,255,0.2);
  }
  
  .bubble-1 {
    width: 80px;
    height: 80px;
    background: 
      radial-gradient(circle at 30% 30%, rgba(255,255,255,0.8) 0%, transparent 20%),
      radial-gradient(circle, rgba(147,51,234,0.7) 0%, rgba(168,85,247,0.5) 40%, rgba(196,181,253,0.3) 70%, transparent 100%);
    left: 10%;
    animation: bubbleFloat1 8s infinite ease-in-out;
    animation-delay: 0s;
  }
  
  .bubble-2 {
    width: 60px;
    height: 60px;
    background: 
      radial-gradient(circle at 25% 25%, rgba(255,255,255,0.9) 0%, transparent 15%),
      radial-gradient(circle, rgba(168,85,247,0.8) 0%, rgba(196,181,253,0.6) 35%, rgba(221,214,254,0.4) 65%, transparent 100%);
    left: 70%;
    animation: bubbleFloat2 10s infinite ease-in-out;
    animation-delay: 2s;
  }
  
  .bubble-3 {
    width: 100px;
    height: 100px;
    background: 
      radial-gradient(circle at 35% 25%, rgba(255,255,255,0.7) 0%, transparent 25%),
      radial-gradient(circle, rgba(124,58,237,0.6) 0%, rgba(147,51,234,0.5) 45%, rgba(168,85,247,0.3) 75%, transparent 100%);
    left: 30%;
    animation: bubbleFloat3 12s infinite ease-in-out;
    animation-delay: 4s;
  }
  
  .bubble-4 {
    width: 40px;
    height: 40px;
    background: 
      radial-gradient(circle at 20% 20%, rgba(255,255,255,1) 0%, transparent 10%),
      radial-gradient(circle, rgba(196,181,253,0.9) 0%, rgba(221,214,254,0.7) 30%, rgba(237,233,254,0.5) 60%, transparent 100%);
    left: 85%;
    animation: bubbleFloat1 9s infinite ease-in-out;
    animation-delay: 1s;
  }
  
  .bubble-5 {
    width: 70px;
    height: 70px;
    background: 
      radial-gradient(circle at 30% 20%, rgba(255,255,255,0.8) 0%, transparent 18%),
      radial-gradient(circle, rgba(139,92,246,0.7) 0%, rgba(168,85,247,0.5) 40%, rgba(196,181,253,0.3) 70%, transparent 100%);
    left: 50%;
    animation: bubbleFloat2 11s infinite ease-in-out;
    animation-delay: 3s;
  }
  
  .bubble-6 {
    width: 50px;
    height: 50px;
    background: 
      radial-gradient(circle at 25% 30%, rgba(255,255,255,0.9) 0%, transparent 12%),
      radial-gradient(circle, rgba(147,51,234,0.8) 0%, rgba(168,85,247,0.6) 35%, rgba(196,181,253,0.4) 65%, transparent 100%);
    left: 15%;
    animation: bubbleFloat3 7s infinite ease-in-out;
    animation-delay: 5s;
  }
  
  .bubble-7 {
    width: 35px;
    height: 35px;
    background: 
      radial-gradient(circle at 20% 25%, rgba(255,255,255,1) 0%, transparent 8%),
      radial-gradient(circle, rgba(168,85,247,0.9) 0%, rgba(196,181,253,0.7) 25%, rgba(221,214,254,0.5) 55%, transparent 100%);
    left: 25%;
    animation: bubbleFloat1 6s infinite ease-in-out;
    animation-delay: 1.5s;
  }
  
  .bubble-8 {
    width: 90px;
    height: 90px;
    background: 
      radial-gradient(circle at 35% 30%, rgba(255,255,255,0.7) 0%, transparent 22%),
      radial-gradient(circle, rgba(124,58,237,0.6) 0%, rgba(139,92,246,0.5) 42%, rgba(168,85,247,0.3) 72%, transparent 100%);
    left: 60%;
    animation: bubbleFloat2 13s infinite ease-in-out;
    animation-delay: 6s;
  }
  
  .bubble-9 {
    width: 45px;
    height: 45px;
    background: 
      radial-gradient(circle at 22% 28%, rgba(255,255,255,0.95) 0%, transparent 10%),
      radial-gradient(circle, rgba(196,181,253,0.8) 0%, rgba(221,214,254,0.6) 32%, rgba(237,233,254,0.4) 62%, transparent 100%);
    left: 5%;
    animation: bubbleFloat3 8.5s infinite ease-in-out;
    animation-delay: 2.5s;
  }
  
  .bubble-10 {
    width: 65px;
    height: 65px;
    background: 
      radial-gradient(circle at 28% 25%, rgba(255,255,255,0.8) 0%, transparent 16%),
      radial-gradient(circle, rgba(147,51,234,0.7) 0%, rgba(168,85,247,0.5) 38%, rgba(196,181,253,0.3) 68%, transparent 100%);
    left: 80%;
    animation: bubbleFloat1 10.5s infinite ease-in-out;
    animation-delay: 4.5s;
  }
  
  .bubble-11 {
    width: 55px;
    height: 55px;
    background: 
      radial-gradient(circle at 26% 22%, rgba(255,255,255,0.85) 0%, transparent 14%),
      radial-gradient(circle, rgba(139,92,246,0.75) 0%, rgba(168,85,247,0.55) 36%, rgba(196,181,253,0.35) 66%, transparent 100%);
    left: 40%;
    animation: bubbleFloat2 9.5s infinite ease-in-out;
    animation-delay: 7s;
  }
  
  .bubble-12 {
    width: 75px;
    height: 75px;
    background: 
      radial-gradient(circle at 32% 28%, rgba(255,255,255,0.75) 0%, transparent 20%),
      radial-gradient(circle, rgba(124,58,237,0.65) 0%, rgba(147,51,234,0.5) 44%, rgba(168,85,247,0.3) 74%, transparent 100%);
    left: 90%;
    animation: bubbleFloat3 11.5s infinite ease-in-out;
    animation-delay: 3.5s;
  }
  
  @keyframes bubbleFloat1 {
    0% {
      transform: translateY(100vh) scale(0);
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    90% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100px) scale(1);
      opacity: 0;
    }
  }
  
  @keyframes bubbleFloat2 {
    0% {
      transform: translateY(100vh) translateX(0) scale(0);
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    50% {
      transform: translateY(50vh) translateX(20px) scale(1);
    }
    90% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100px) translateX(-20px) scale(1);
      opacity: 0;
    }
  }
  
  @keyframes bubbleFloat3 {
    0% {
      transform: translateY(100vh) translateX(0) scale(0);
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    30% {
      transform: translateY(70vh) translateX(-15px) scale(0.8);
    }
    70% {
      transform: translateY(30vh) translateX(15px) scale(1.2);
    }
    90% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100px) translateX(0) scale(1);
      opacity: 0;
    }
  }
</style>
